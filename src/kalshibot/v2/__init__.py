"""Kalshibot v2: focused alpha stack with strict risk controls.

Layered on top of the existing codebase. Invoke via `make paper` / `make run`
/ `make backtest`. See DECISIONS.md for why the architecture diverges from
the legacy 151-strategy ensemble.
"""
__version__ = "2.0.0"
